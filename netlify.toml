[build]
  base = ""
  command = "npm --prefix ui install && npm --prefix ui run build"
  publish = "ui/dist"
  functions = "api"

[dev]
  functions = "api-local"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

